# include <stdio.h>
# include "main.h"
/**
 * print_alphabet_x10 - guess it
 *
 * Return: Always 0 (Succeess)
*/

int _islower(int i)
{
	if (i >='a' && i <= 'z')
		return (1);
	else 
		return (0);

}
